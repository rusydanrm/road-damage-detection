# requirements.txt
# Dependencies untuk Flask Backend

# Flask dan ekstensi
Flask==3.0.0
Flask-CORS==4.0.0
Werkzeug==3.0.1

# TensorFlow dan Keras untuk model CNN
# Untuk CPU only:
tensorflow==2.15.0
keras==2.15.0

# Image Processing
Pillow==10.1.0

# NumPy untuk operasi array
numpy==1.26.4

# Gunicorn untuk production server (opsional)
gunicorn==26.0.0
